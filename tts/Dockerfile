# Future: Piper, piper-plus, or VOICEVOX for server-side TTS inside Docker.
FROM ubuntu:24.04

RUN apt-get update && apt-get install -y \
    ca-certificates \
    curl \
    && rm -rf /var/lib/apt/lists/*

CMD ["sh", "-c", "echo 'TTS container placeholder. Piper or VOICEVOX will be added later.' && sleep infinity"]
